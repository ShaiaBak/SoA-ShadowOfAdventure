using UnityEngine;
using System.Collections;

public class PlayerInfo : MonoBehaviour {

    private static PlayerLocation location;
    public static PlayerLocation getLocation {

        get { return location; }
    }

    // this class needs references to inventory(if iplicable)
    // quest list
    // stats
}
